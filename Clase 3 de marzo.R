x <- runif(1, min=0, max=100)
    if(x<=30) {
    y <- "Hola"
} else if(x>30 && x<50) {
    y <- "Qué onda"
} else if(x>50 && x<60) {
    y <- "Quiubo"
} else if(x>60 && x<80) {
    y <- "Qué pex"
} else if (x>80 && x<100) {
    y <- "Qué pedo wey"
}
y